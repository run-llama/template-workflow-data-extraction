name: "test-proj"

control-plane:
  port: 8000

default-service: process-file

services:
  process-file:
    name: Process File
    path: "src/test_proj.process_file:workflow"
    env_files:
    - .env

ui:
  name: "test-proj UI"
  source:
    name: ./ui
